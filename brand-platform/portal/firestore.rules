rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All portal and mobile API access uses Firebase Admin SDK on the server.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
